{
  "name": "publish-factory-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to publish an ElasticClaw factory to a hub",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
